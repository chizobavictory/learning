import React from "react";

const Learning = (children) => {
    return (
        <div style={{backgroundColor : 'red', margin: '10px'}}>
            {children}
        </div>
    )
}
export default Learning